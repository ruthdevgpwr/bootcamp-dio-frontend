// Verifique se o número é par utilizando a estrutura condicional if

const numero = 485;

const isNumeroPar = numero % 2 === 0;

if (isNumeroPar) {
  console.log(`O número ${numero} é Par`);
} else {
  console.log(`O número ${numero} é Ímpar`);
}
